ولا يهمك، بما إنك نزّلتي XAMPP نقدرو نشتغلو عليه عادي ونخلو Laravel يشتغل على الشبكة الداخلية بدون مشاكل.

خلّيني نعطيك الخطوات الواضحة بحيث النظام يشتغل فورًا.

⸻

✅ 1) شغّلي Apache + MySQL

من XAMPP Control Panel:
	•	Start لـ Apache
	•	Start لـ MySQL

لو Apache ما اشتغل، غالبًا بورت 80 محجوز… نحلّها لو صارت.

⸻

✅ 2) حطي مشروع Laravel في htdocs

انسخي مشروعك كامل إلى:

C:\xampp\htdocs\project

واسم “project” غيريه لاسم مشروعك الحقيقي.

⸻

✅ 3) نزّلي Composer

حتى نقدر نثبت مكتبات Laravel:

بعد التثبيت، افتحي CMD داخل مجلد المشروع ونفذي:

composer install
copy .env.example .env
php artisan key:generate

عدّلي إعدادات قاعدة البيانات داخل ملف:

.env


⸻

✅ 4) خليه يشتغل على الشبكة الداخلية

افتح ملف:

C:\xampp\apache\conf\extra\httpd-vhosts.conf

وضيفي Virtual Host:

<VirtualHost *:80>
    DocumentRoot "C:/xampp/htdocs/project/public"
    DirectoryIndex index.php
    <Directory "C:/xampp/htdocs/project/public">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

بعدها:
	•	شوفي Apache → Restart.

⸻

✅ 5) تأكدي من صلاحية mod_rewrite

لكن عادة مفعّل تلقائيًا في XAMPP.

افتحي ملف:

C:\xampp\apache\conf\httpd.conf

وضبّطي السطر:

LoadModule rewrite_module modules/mod_rewrite.so

تأكدي مش متعلّق بـ #.

⸻

✅ 6) الدخول من أجهزة الشبكة

شوفي IP جهاز السيرفر:

مثال:

192.168.1.50

المستخدمين يدخلوا هكي:

http://192.168.1.50

ولو تبّيه باسم:

http://intranet.local

نقدر نركّب DNS داخلي أو نعدّل hosts — نوضحها لو تبي.

⸻

🎯 جاهزة نمشّيك خطوة خطوة حسب جهازك؟

لو تعطيني:
	•	IP للجهاز اللي عليه XAMPP
	•	اسم مجلد مشروع Laravel
	•	هل تبي دخول عبر IP أو اسم (domain)؟

نركّب لك الإعداد كامل جاهز.